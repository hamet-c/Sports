# Minimal deps for the CI daily-ingest job (ingest_props + refresh_injuries).
# The full ML stack in requirements.txt is not needed and would add minutes
# to every scheduled run. Pin Python 3.12 in CI: libsql-experimental (pulled
# by sqlalchemy-libsql) has manylinux wheels for cp312 but not cp314.
sqlalchemy>=2.0.0
sqlalchemy-libsql>=0.2.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
httpx>=0.27.0
loguru>=0.7.0
tenacity>=8.2.0
